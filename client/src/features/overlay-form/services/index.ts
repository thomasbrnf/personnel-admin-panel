import { initialState, initialPersonData } from "./CONSTANTS";
import { setMainData, filterOutOnEmpty } from "./data.service";
import { submitData } from "./person.service";

export {
  initialState,
  initialPersonData,
  setMainData,
  filterOutOnEmpty,
  submitData,
};
